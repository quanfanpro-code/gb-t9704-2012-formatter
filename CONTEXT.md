# 公文排版领域

本项目处理普通材料和一般正式公文的排版，并对最终输出文件给出可追溯的检查结论。

## 领域语言

**普通材料**：
报告、方案、汇报等参照公文版式整理的材料，选择该模式本身不表示正式发文，也不自动增加红头。
_避免使用_：自动红头文件

**一般普通公文**：
不采用信函、命令、纪要专属格式，也不包含上行文签发人版式的公文。
_避免使用_：全类型公文、任意公文

**正式公文**：
本项目中由使用者明确选择正式发文版式的一般公文，包含相应版头、主体、版记和页码。本期范围不含上行文签发人版式、联合行文和特定文种格式。
_避免使用_：任意正式文件

**电子红头**：
文档中包含红色发文机关标志和红色分隔线的完整电子版式。
_避免使用_：红头纸套打

**预印红头纸套打**：
将本次内容打印到已有机关标志和红线的纸张上，文档为纸上已有要素预留位置。
_避免使用_：重新打印红头

**公文要素**：
机构名称、发文字号、标题、主送机关、署名、日期、附件说明等具有明确公文用途的信息。
_避免使用_：可随意补齐的默认信息

**安全排版**：
只调整能够可靠识别且不会破坏内容结构的版式属性；复杂内容保持原样并转人工确认。
_避免使用_：全文重写、智能清洗

**阻断错误**：
足以证明输出文件不完整、不可用或结构无效的问题。存在阻断错误时，程序不得发布正式输出，也不得给出“通过”。
_避免使用_：普通警告、提示

**人工确认事项**：
程序无法在不损害内容的前提下可靠判断或自动修正的项目。它不等同于阻断错误，但合规结论不能忽略它。
_避免使用_：已通过、已修复

**正式输出**：
完成排版并通过结构校验后，以唯一文件名发布给用户的 DOCX 文件。
_避免使用_：临时文件、中间文件

**最终检查**：
针对正式输出候选文件执行的结构和业务规则检查，是检查报告的唯一事实来源。
_避免使用_：输入预检、格式化前检查

**Word 版面实测**：
在 Microsoft Word 中使用指定的实际字体重新分页，对最终可见版面、字体及关键位置取得可复核证据的检查。
_避免使用_：仅文件结构检查、替代字体通过
